FILE(GLOB SRC *.cpp)

SET(SOURCES ${SOURCES} ${SRC} PARENT_SCOPE)
SET(LIBS ${FFMPEG_LIBRARIES} z bz2)

IF(WIN32)
SET(LIBS ${LIBS} vfw32)
ENDIF()

SET(DEPEND_LIBS ${DEPEND_LIBS} ${LIBS} PARENT_SCOPE)