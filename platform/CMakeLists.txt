add_subdirectory(posix)

set(SOURCES ${SOURCES} ${PLATFORM_SOURCES} PARENT_SCOPE)

IF(WIN32)
SET(LIBS wsock32 pthread)
ENDIF()
set(DEPEND_LIBS ${DEPEND_LIBS} ${LIBS} ${PLATFORM_DEPEND_LIBS} PARENT_SCOPE)