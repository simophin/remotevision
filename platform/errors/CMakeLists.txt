IF(WIN32)
SET(SRCS ${CMAKE_CURRENT_SOURCE_DIR}/Win32Errors.cpp)
ENDIF()

IF(UNIX)
SET(SRCS  ${CMAKE_CURRENT_SOURCE_DIR}/PosixErrors.cpp)
ENDIF()


SET(PLATFORM_SOURCES ${PLATFORM_SOURCES} ${SRCS} PARENT_SCOPE)